enum UserType { driver, business }

enum DriverApprovalStatus { pending, approved, blocked, rejected }

enum BusinessApprovalStatus { blocked, unBlocked, approved, pending }

enum VehicleType { all, car, bike }

enum DriverRequestCategory { favorite, others }

enum RequestDecision { accept, decline }

enum PaymentType { cash, card }
